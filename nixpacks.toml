[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "php82Extensions.pdo_pgsql"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.start]
cmds = ["php artisan migrate --force", "php artisan serve --host=0.0.0.0 --port=$PORT"]
